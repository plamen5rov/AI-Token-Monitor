# Encryption key for API keys stored in the database (AES-256-GCM).
# Generate one with: openssl rand -hex 32
# If not set, a default key is used (NOT secure — for development only).
ENCRYPTION_KEY=

# Path to the SQLite database file. Defaults to ./atm.sqlite if not set.
# DATABASE_PATH=./atm.sqlite
