# DONE.md

Changelog for the AI Token Monitor (ATM) project.

---

## Phase 0 — Project Documentation

- [2026-07-05] Reorganized project documentation: moved spec files into `docs/agents/`, `docs/architecture/`, and `docs/ui/`; added Documentation Structure section to `AGENTS.md`; fixed internal cross-references in agent docs (files modified: AGENTS.md, docs/agents/*, docs/architecture/*, docs/ui/*)
- [2026-07-05] Added Agent Workflow Rules to `AGENTS.md` and agent instruction docs: detect inconsistencies and ask user, keep docs in sync with decisions, log all changes in `DONE.md` (files modified: AGENTS.md, docs/agents/OPENODE_MASTER_PROMPT.md, docs/agents/OPENODE_BUILD_AGENT.md, DONE.md)
- [2026-07-05] Refined Agent Workflow Rules: newer software package versions and working code prevail over pre-planning docs; UI and colors are iterated from a working app, not `.md` files; mirrored the principles into global OpenCode config at `~/.config/opencode/AGENTS.md` (files modified: AGENTS.md, docs/agents/OPENODE_MASTER_PROMPT.md, docs/agents/OPENODE_BUILD_AGENT.md, ~/.config/opencode/AGENTS.md, DONE.md)

---

## Phase 1 — Project Foundation

- [2026-07-05] Initialized Next.js 16 + TypeScript + Tailwind CSS v4 project at repo root; installed shadcn/ui (base-nova preset), `better-sqlite3`, and `next-themes`; created base folder structure (`app/`, `components/`, `lib/`, `providers/`, `templates/`, `database/`, `migrations/`, `types/`); built sidebar/top navigation shell with dark-mode-default theme toggle; verified clean production build with no TypeScript errors (files modified: .gitignore, package.json, package-lock.json, tsconfig.json, next.config.ts, postcss.config.mjs, eslint.config.mjs, app/layout.tsx, app/page.tsx, app/globals.css, components/theme-provider.tsx, components/theme-toggle.tsx, components/app-shell.tsx, components/ui/button.tsx, lib/utils.ts, components.json; directories created: app, components, lib, providers, templates, database, migrations, types)

---

## Phase 2 — Database Layer

- [2026-07-05] Implemented SQLite database layer using `better-sqlite3`: created migration runner, initial schema SQL for all 6 tables, connection module with WAL and foreign keys, TypeScript entity types, and CRUD helpers for providers/models/usage records/settings; verified initialization and CRUD operations with `scripts/verify-db.ts`; installed `@types/better-sqlite3` and `tsx` dev dependencies (files modified: package.json, package-lock.json, database/index.ts, database/migrate.ts, migrations/001_initial_schema.sql, lib/db.ts, types/index.ts, scripts/verify-db.ts, DONE.md)

---

## Phase 3 — Provider Integration Framework

- [2026-07-05] Implemented provider integration framework: AES-256-GCM API key encryption (`lib/crypto.ts`), `ProviderAdapter` interface and `BaseProvider` abstract class (`providers/base.ts`), OpenAI adapter with `/v1/models` and `/v1/organization/usage/completions` endpoints and hardcoded pricing map for common models (`providers/openai.ts`), adapter factory (`providers/index.ts`), sync log and `getModelByName` CRUD helpers added to `lib/db.ts`, sync engine orchestrating model upsert, usage record creation with cost computation, and sync_log lifecycle (`lib/sync.ts`); verified full sync flow with a mock provider adapter via `scripts/verify-providers.ts` — all assertions passed (models: gpt-4o, gpt-4o-mini; records: 2; total cost: $72.30; sync log status: success; provider.last_sync updated) (files modified: lib/db.ts, lib/crypto.ts, lib/sync.ts, providers/base.ts, providers/openai.ts, providers/index.ts, types/index.ts, scripts/verify-providers.ts, next-env.d.ts, DONE.md)

---

## Phase 4 — Synchronization Engine

- [2026-07-05] Implemented synchronization engine: duplicate protection for usage records via unique index `(provider_id, model_id, timestamp)` with `ON CONFLICT DO UPDATE` replace semantics (`migrations/002_usage_records_dedupe.sql`, updated `createUsageRecord` in `lib/db.ts`); pre-aggregated `usage_daily` rollup populated during sync via `rebuildUsageDaily()` plus `getUsageDaily()` and `getDashboardTotals()` query helpers; sync flow in `lib/sync.ts` now rebuilds daily aggregates after each provider sync; server actions `syncAllAction` and `syncProviderAction` (`app/actions/sync.ts`); reusable client `SyncAllButton`/`SyncOneButton` components (`components/sync-button.tsx`); wired header Sync button to call `syncAllAction`; built sync history page at `/sync` (force-dynamic server component) showing per-provider sync buttons + history table with status badges and durations; added "Sync" nav entry; extended `.gitignore` for `*.sqlite-shm`/`*.sqlite-wal`; verified idempotency and replace semantics end-to-end with `scripts/verify-sync.ts` (re-syncing same bucket does not double counts; growing bucket replaces old value). MVP dedupe decision noted: daily-bucket providers only; per-request providers (Phase 6) will need a different dedupe key. (files modified: components/app-shell.tsx, lib/db.ts, lib/sync.ts, .gitignore, DONE.md; files added: migrations/002_usage_records_dedupe.sql, app/actions/sync.ts, app/sync/page.tsx, components/sync-button.tsx, scripts/verify-sync.ts)

---

## Phase 5 — Dashboard & Analytics

- [2026-07-05] Built analytics dashboard with live data: installed `recharts` (v3.9.2); added dashboard query helpers in `lib/db.ts` — `getDailySeries`, `getMonthlySeries`, `getProviderBreakdown`, `getModelBreakdown`, `getRecentActivity`, `getActiveProvidersCount`, `getRequestsTodayCount`; added `lib/format.ts` (currency, number, timestamp, relative-time formatters); created `components/dashboard/` directory with `MetricCard`, `CostTrendChart` (area chart with daily/monthly toggle), `TokenTrendChart` (stacked area, input vs output), `ProviderBreakdown`, `ModelBreakdown`, `RecentActivity` tables; rewrote `app/page.tsx` as a force-dynamic server component that fetches all data server-side and passes to chart/table components; charts use shadcn v4 oklch CSS variables (`var(--primary)` etc.) for theme-aware colors; all sections include empty-state messaging for fresh installs; verified all queries with `scripts/verify-dashboard.ts` (seeded 90 days × 3 models, confirmed totals, daily/monthly series, provider/model breakdowns, recent activity ordering); build passes clean (route `/` is dynamic `ƒ`). (files modified: app/page.tsx, lib/db.ts, DONE.md, package.json, package-lock.json; files added: lib/format.ts, components/dashboard/metric-card.tsx, components/dashboard/cost-trend-chart.tsx, components/dashboard/token-trend-chart.tsx, components/dashboard/provider-breakdown.tsx, components/dashboard/model-breakdown.tsx, components/dashboard/recent-activity.tsx, scripts/verify-dashboard.ts)

---

## Phase 6 — Multi-Provider Support

- [2026-07-05] Expanded ATM to support three providers (OpenAI, Anthropic, OpenRouter) with full provider management UI: created `templates/index.ts` with `ProviderTemplate` type and template definitions for all 3 providers (name, base URL, auth method, API key prefix/label/help URL, supported endpoints); created `providers/anthropic.ts` adapter (hardcoded model list with per-1K pricing from Claude Platform docs, fetches usage from `/v1/organizations/usage_report/messages` with `group_by[]=model` and `bucket_width=1d`, handles pagination via `next_page`, maps `uncached_input_tokens` + `cached_input_tokens` + `cache_creation_input_tokens` + `cache_read_input_tokens` → input tokens, computes cost from pricing map, custom `healthCheck()` via usage endpoint with `x-api-key` + `anthropic-version: 2023-06-01` headers); created `providers/openrouter.ts` adapter (fetches models from `GET /api/v1/models` with per-token pricing converted to per-1K, fetches usage from `GET /api/v1/activity` which returns last 30 days with `prompt_tokens`, `completion_tokens`, `reasoning_tokens`, `requests`, and `usage` (cost in USD) per model per day, uses cost directly from API, custom `healthCheck()` via `GET /api/v1/key`); updated `providers/index.ts` factory to handle `"anthropic"` and `"openrouter"` types; added `requestCount` optional field to `NormalizedUsageRecord` type; updated `lib/sync.ts` `syncUsage()` to use `record.requestCount || 1` instead of hardcoded `1`; updated `providers/openai.ts` to pass `num_model_requests` as `requestCount`; created `app/actions/providers.ts` server actions (`addProviderAction`, `deleteProviderAction`, `toggleProviderAction`, `healthCheckAction`, `syncProviderFromProvidersAction`); created `components/provider-form.tsx` (client component with template selector, name input, password-type API key input with help link, inline validation); created `components/provider-actions.tsx` (client component with per-provider Sync, Health Check, Toggle, Delete buttons using `useTransition`); created `app/providers/page.tsx` (force-dynamic server component listing all providers with type badges, status badges, last sync, and action buttons; empty state with template previews); verified with `scripts/verify-providers-phase6.ts` (22 assertions: templates, CRUD for all 3 types, API key encryption/decryption, adapter factory, toggle, delete, multi-provider dashboard aggregation, provider breakdown, disable-doesn't-remove-data, sync-filters-active, Anthropic hardcoded models); build passes clean (`/`, `/providers`, `/sync` all dynamic `ƒ`). (files added: templates/index.ts, providers/anthropic.ts, providers/openrouter.ts, app/actions/providers.ts, components/provider-form.tsx, components/provider-actions.tsx, app/providers/page.tsx, scripts/verify-providers-phase6.ts; files modified: providers/index.ts, providers/openai.ts, types/index.ts, lib/sync.ts, DONE.md)

---

## Phase 7 — MVP Polish & Release Readiness

- [2026-07-05] Final polish for MVP release: added error boundary `app/error.tsx` (client component with alert icon, error message, retry button); added 404 page `app/not-found.tsx` (compass icon, link back to dashboard); created `components/skeletons.tsx` with `MetricCardSkeleton`, `ChartSkeleton`, `TableSkeleton`, `DashboardSkeleton` using `animate-pulse`; added `app/loading.tsx` (dashboard skeleton), `app/sync/loading.tsx`, `app/providers/loading.tsx` (route-level loading states); installed `sonner` (v2.0.7) for toast notifications; created `components/toaster.tsx` (theme-aware Sonner toaster with card styling); wired `<Toaster />` into `app/layout.tsx`; refactored `components/sync-button.tsx` to use `toast.success`/`toast.error` instead of inline messages; refactored `components/provider-actions.tsx` to use toasts + `router.refresh()` for automatic UI updates after sync/toggle/delete; refactored `components/provider-form.tsx` to use toasts for validation errors and success feedback; created `.env.example` with `ENCRYPTION_KEY` and `DATABASE_PATH` documentation; replaced default Next.js README with full project README (features, tech stack, quick start, configuration, API key requirements table, usage guide, project structure, architecture, database schema overview, verification scripts); fixed 6 lint warnings (removed unused imports in `app/providers/loading.tsx`, `scripts/verify-providers-phase6.ts`, removed unused `AUTH_TAG_LENGTH` constant in `lib/crypto.ts`); verified fresh install (deleted DB, started production server on port 3001, all pages return HTTP 200 with proper empty states: dashboard shows "Overview"/"Total Cost"/"Active Providers", providers page shows "No providers configured" with template previews, sync page shows "No sync runs", 404 page returns HTTP 404); verified fresh DB schema (all 6 tables + `_migrations` + all indexes including dedupe index created correctly); build passes clean (0 errors, 0 warnings); lint passes clean. (files added: app/error.tsx, app/not-found.tsx, app/loading.tsx, app/sync/loading.tsx, app/providers/loading.tsx, components/skeletons.tsx, components/toaster.tsx, .env.example; files modified: app/layout.tsx, components/sync-button.tsx, components/provider-actions.tsx, components/provider-form.tsx, lib/crypto.ts, scripts/verify-providers-phase6.ts, README.md, DONE.md; dependency added: sonner@2.0.7)
- [2026-07-05] Doc sync after provider architecture refactor: updated README (line 134 "OpenAI, Anthropic, OpenRouter, or NVIDIA NIM" → "14 providers available"; provider adapters list now includes OpenAICompatible); updated AGENTS.md — `Provider Template Concept` (removed per-file template example, added note about single `templates/index.ts` and 14 current templates); `Provider Adapters` (removed `fetchPricing()`/`fetchCosts()` from interface — never implemented; added explanation of OpenAICompatibleProvider shared adapter vs dedicated adapters); `Dashboard Requirements` (added Usage/Settings views with feature bullets, extended Model View with pricing and context window); `Provider Template Rules` (split into two flows — OpenAI-compatible 3-step vs dedicated adapter 5-step); updated `docs/architecture/SKILL.md` (added NVIDIA NIM, DeepSeek, DeepInfra, Anyscale, xAI to OpenAI-Compatible Pattern list); updated `docs/architecture/DATA_FLOW.md` (provider list expanded to all 14); updated `docs/agents/OPENODE_MASTER_PROMPT.md` (project overview provider list expanded). (files modified: README.md, AGENTS.md, docs/architecture/SKILL.md, docs/architecture/DATA_FLOW.md, docs/agents/OPENODE_MASTER_PROMPT.md)
- [2026-07-05] Provider architecture refactor + 9 new OpenAI-compatible templates: created `providers/openai-compatible.ts` generic `OpenAICompatibleProvider` adapter that reads `baseUrl` and `authMethod` from the provider template (via `getTemplate()`) — supports Bearer, `x-api-key`, and `x-goog-api-key` auth methods; `fetchModels()` hits `${baseUrl}/models` and maps OpenAI-format `{ data: [{ id, ... }] }` responses; `fetchUsage()` returns `[]` (these providers have no public usage analytics endpoint); `healthCheck()` via `/models`; refactored `providers/index.ts` factory to route OpenAI-compatible types through the generic adapter via `OPENAI_COMPATIBLE_TYPES` set; deleted `providers/nvidia.ts` (NVIDIA NIM now handled by the generic adapter); added 9 new provider templates: **Groq** (`api.groq.com/openai/v1`), **Mistral** (`api.mistral.ai/v1`), **Together AI** (`api.together.xyz/v1`), **DeepSeek** (`api.deepseek.com/v1`), **Fireworks AI** (`api.fireworks.ai/inference/v1`), **Perplexity** (`api.perplexity.ai`), **DeepInfra** (`api.deepinfra.com/v1/openai`), **Anyscale** (`api.endpoints.anyscale.com/v1`), and **xAI** (`api.x.ai/v1`) — all with `supportsUsage: false`; extended `authMethod` union to include `"x-goog-api-key"` (needed for Google AI Studio); added badge colors for all 9 new provider types on the Providers page; updated README with the full 14-template Provider Support matrix, expanded API key requirements table (14 rows), and a new **Adding OpenAI-Compatible Providers** section documenting how to add a new template in 3 steps (template entry + factory set membership + optional badge color). (files modified: templates/index.ts, providers/index.ts, app/providers/page.tsx, README.md, DONE.md; files added: providers/openai-compatible.ts; files deleted: providers/nvidia.ts)
- [2026-07-05] Google AI Studio provider + earlier post-release changes: created `providers/google.ts` adapter — `fetchModels()` calls `${GOOGLE_BASE_URL}/models` with `x-goog-api-key` header, filters to `generateContent`-compatible models, strips `models/` prefix; `fetchUsage()` returns `[]` (Gemini API has no public REST usage endpoint, analytics only at aistudio.google.com/usage); custom `healthCheck()` via `/models`; added `google` template (`baseUrl: generativelanguage.googleapis.com/v1beta`, `apiKeyPrefix: AIza`); registered `GoogleProvider` in factory; added red Google badge color. Earlier in the same session: created `app/settings/page.tsx` with refresh interval + currency form (server action `saveSettingsAction`); created `app/models/page.tsx` with per-model pricing + usage table; created loading skeletons for both; added NVIDIA NIM template (since replaced by generic adapter); features list and Provider Support matrix updates. (files modified: templates/index.ts, providers/index.ts, app/providers/page.tsx; files added: providers/google.ts, app/actions/settings.ts, components/settings-form.tsx, app/models/page.tsx, app/models/loading.tsx, app/settings/page.tsx, app/settings/loading.tsx)
- [2026-07-05] Post-release page additions + NVIDIA NIM provider + README redesign: created `app/settings/page.tsx` with refresh interval + currency form (server action `saveSettingsAction` in `app/actions/settings.ts`, `components/settings-form.tsx` client component with toast feedback); created `app/settings/loading.tsx` skeleton; created `app/models/page.tsx` with per-model pricing + usage table (context window, $/1k prices, token totals, requests, cost) sorted by cost; created `app/models/loading.tsx` skeleton; added NVIDIA NIM provider template (`templates/index.ts`) — `baseUrl: integrate.api.nvidia.com/v1`, `apiKeyPrefix: nvapi-`, `supportsModels: true`, `supportsUsage: false`; created `providers/nvidia.ts` adapter — `fetchModels()` pulls OpenAI-compatible `/models` list, `fetchUsage()` returns `[]` (NVIDIA NIM is inference-only, no public usage endpoint), custom `healthCheck()` via `/models`; registered `NvidiaProvider` in `providers/index.ts` factory; added lime-green NVIDIA badge color and "Models only" warning badge on Providers page for providers with `supportsUsage: false`; redesigned README with badges, screenshot placeholder, expanded features list covering all 6 dashboard pages, full env var reference table, Provider Support matrix (Models/Usage/Cost per provider), all 6 ERROR-LOG entries in Troubleshooting section, data flow pipeline. (files modified: templates/index.ts, providers/index.ts, README.md, app/providers/page.tsx, DONE.md; files added: providers/nvidia.ts, app/settings/page.tsx, app/settings/loading.tsx, app/models/page.tsx, app/models/loading.tsx, app/actions/settings.ts, components/settings-form.tsx)
- [2026-07-05] Post-release bug fixes from user testing: fixed `ReferenceError: SyncResult is not defined` (HTTP 500 on add provider) by removing invalid `export type { SyncResult }` re-export from `"use server"` file `app/actions/sync.ts`; added `sanitizeApiKey()` in `app/actions/providers.ts` to strip zero-width spaces (U+200B–U+200F), BOM (U+FEFF), and non-breaking spaces (U+00A0) from pasted API keys before storage (was causing `ByteString` conversion error in HTTP headers); fixed `FOREIGN KEY constraint failed` on provider delete by updating `deleteProvider()` in `lib/db.ts` to delete child rows (`usage_records`, `usage_daily`, `models`, `sync_log`) in a transaction before the provider row; improved OpenRouter 403 error message to mention Management key requirement; added `autoComplete="off"` to API key input in `components/provider-form.tsx`; fixed `NOT NULL constraint failed: usage_records.timestamp` in OpenRouter adapter by skipping items with missing/invalid dates and adding defensive `Number.isFinite(timestamp)` check in `lib/sync.ts`; switched OpenAI adapter from `/v1/models` API call (which rejects Admin keys with 403) to hardcoded model list with pricing (same pattern as Anthropic), added o1/o3-mini models to pricing map, added pagination support via `next_page`, added custom `healthCheck()` via usage endpoint, added timestamp validation; updated OpenAI template to require Admin API key (`sk-admin-...`); updated README API key requirements table to show all 3 providers require admin/management keys; created `app/usage/page.tsx` with per-model token/cost breakdown table (KPI cards, charts, sortable per-model table with totals footer) and `app/usage/loading.tsx` skeleton; fixed `app/not-found.tsx` to use plain `<Link>` instead of `<Button render={...}>` to resolve Base UI accessibility warning. (files modified: app/actions/sync.ts, app/actions/providers.ts, lib/db.ts, lib/sync.ts, providers/openai.ts, providers/openrouter.ts, templates/index.ts, components/provider-form.tsx, app/not-found.tsx, README.md, ERROR-LOG.md, DONE.md; files added: app/usage/page.tsx, app/usage/loading.tsx)

---

## Process & Tooling

- [2026-07-05] Created `ERROR-LOG.md` at project root and added error-logging principle to workflow rules; logged first entry about Dark Reader hydration mismatch; mirrored the principle into global OpenCode config at `~/.config/opencode/AGENTS.md` (files modified: ERROR-LOG.md, AGENTS.md, docs/agents/OPENODE_MASTER_PROMPT.md, docs/agents/OPENODE_BUILD_AGENT.md, ~/.config/opencode/AGENTS.md, DONE.md)

## Phase 8 — OpenCode Zen Provider

- [2026-07-07] Added OpenCode Zen as the 15th provider template (model-registry-only): added `opencode` template in `templates/index.ts` with `baseUrl: https://opencode.ai/zen/v1`, Bearer auth, `apiKeyLabel: "Zen API Key"`, `apiKeyHelpUrl: https://opencode.ai/settings/keys`, `supportsUsage: false`; verified Zen `/v1/models` returns OpenAI-shaped `{ data: [{ id, object, created, owned_by }] }` (HTTP 200 without auth, confirming the generic adapter can parse it); added `"opencode"` to `OPENAI_COMPATIBLE_TYPES` set in `providers/index.ts`; added fuchsia badge color for `opencode` type in `app/providers/page.tsx`; updated README (intro paragraph: "Eleven" → "Twelve", provider count "14" → "15", added OpenCode Zen row to Provider Support matrix and API key requirements table, multi-provider feature bullet list); updated AGENTS.md (template count "14" → "15", template list includes `opencode`, OpenAI-compatible provider list includes `opencode`); updated `docs/architecture/DATA_FLOW.md` provider list to include OpenCode Zen. (files modified: templates/index.ts, providers/index.ts, app/providers/page.tsx, README.md, AGENTS.md, docs/architecture/DATA_FLOW.md, DONE.md)

## Phase 9 — Real Provider Usage Tracking

- [2026-07-07] OpenAI Costs API + model-less usage handling + provider sync transparency: added `/organization/costs` endpoint to OpenAI adapter (`providers/openai.ts`) — fetches exact daily spend (the same data behind platform.openai.com/settings/organization/usage); token rows from `/organization/usage/completions` now store `costUSD: 0` to avoid double-counting estimated vs actual costs; handles model-less rows — when OpenAI returns `model: null`, records go under `__openai_unattributed_usage__` instead of being silently dropped; added two synthetic model entries to OpenAI catalog (`__openai_actual_costs__`, `__openai_unattributed_usage__`); fixed `lib/sync.ts` — when `syncUsage()` encounters a model ID not in the local model table, it now creates a zero-priced model row instead of skipping the record (prevents OpenAI/OpenRouter usage from being silently discarded); added `lastSyncNotes` property to `BaseProvider` (`providers/base.ts`) — adapters can set contextual notes during sync; updated `SyncResult` type (`lib/sync.ts`) with optional `notes` field; OpenRouter adapter (`providers/openrouter.ts`) now pushes a note when `/activity` returns empty (suggests Management key + reporting lag); OpenAI adapter pushes notes when costs or usage return empty; Google adapter and OpenAI-compatible adapter push notes explaining that no public usage API exists and suggesting the ATM gateway; sync action (`app/actions/sync.ts`) surfaces adapter notes in toast messages; updated README, AGENTS.md, DATA_FLOW.md, OPENODE_MASTER_PROMPT.md with OpenAI dual-endpoint architecture and provider-level exact cost preservation principle. (files modified: providers/openai.ts, providers/openrouter.ts, providers/google.ts, providers/openai-compatible.ts, providers/base.ts, lib/sync.ts, app/actions/sync.ts, README.md, AGENTS.md, docs/architecture/DATA_FLOW.md, docs/agents/OPENODE_MASTER_PROMPT.md)

## Phase 10 — Metering Gateway

- [2026-07-07] Built the gateway infrastructure: added `request_logs`, `virtual_keys`, and `budgets` tables via migration `003_request_logs.sql`; added `RequestLog`, `VirtualKey`, `Budget` types to `types/index.ts`; added full CRUD functions for all three tables in `lib/db.ts` (createRequestLog, getRequestLogs, getRequestLogStats, createVirtualKey, getVirtualKeyByHash, createBudget, checkBudgetAllowed, getGatewayDailySeries); created transparent proxy at `app/api/gateway/[provider]/[...path]/route.ts` — POST/GET passthrough with best-effort response parsing for model/token extraction, latency tracking, and request logging; added virtual key auth via `x-atm-key` header (optional — gateway works without keys); budget enforcement via `checkBudgetAllowed()` returns 429 when limits hit; created Gateway dashboard page at `app/gateway/page.tsx` with stats cards (total requests, cost, tokens, avg latency); created `components/gateway/virtual-key-manager.tsx` — create/delete/list virtual keys with provider scoping, budget limits, show/hide key display; created `components/gateway/budget-manager.tsx` — create/delete/list/reset budgets with progress bars and scope selector (global/provider/model); created `components/gateway/request-log-table.tsx` — request log table with provider, model, endpoint, method, status, tokens, latency columns; added Gateway nav item with Network icon to sidebar (`components/app-shell.tsx`); added gateway server actions (`app/actions/gateway.ts`) for virtual key and budget CRUD. (files modified: migrations/003_request_logs.sql, types/index.ts, lib/db.ts, app/api/gateway/[provider]/[...path]/route.ts, app/gateway/page.tsx, components/gateway/virtual-key-manager.tsx, components/gateway/budget-manager.tsx, components/gateway/request-log-table.tsx, components/app-shell.tsx, app/actions/gateway.ts)

- [2026-07-07] OpenCode Local CLI sync adapter: reads usage directly from OpenCode's local SQLite database (`~/.local/share/opencode/opencode.db`); created `providers/opencode-local.ts` — parses JSON-encoded model fields from session table, groups sessions by (model, day) for daily aggregation, imports cost/tokens/timestamps; added `opencode-local` template in `templates/index.ts` with `supportsUsage: true`, `authMethod: "none"`, no API key required; added `"none"` to `authMethod` union type; registered `opencode-local` case in provider factory (`providers/index.ts`); added sky badge color in `app/providers/page.tsx`; made API key field optional in provider form (`components/provider-form.tsx`) and add provider action (`app/actions/providers.ts`) for providers that don't need keys; made `decrypt()` in `lib/crypto.ts` handle empty strings gracefully. (files modified: providers/opencode-local.ts, providers/index.ts, templates/index.ts, app/providers/page.tsx, components/provider-form.tsx, app/actions/providers.ts, lib/crypto.ts)
- [2026-07-07] Fixed OpenCode Local adapter dropping sessions with empty/malformed model IDs ($57.46 of usage was being silently skipped). Sessions with missing model JSON now import under an "unknown" model instead of being filtered out. (files modified: providers/opencode-local.ts)
